{
  "name": "gpt-inventory-system",
  "version": "1.0.0",
  "description": "Web-based inventory system with PostgreSQL-backed API for web and mobile clients",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
